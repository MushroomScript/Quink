# Quink 部署 env 模板. 复制为 .env 后改值. .env 不进 git, 各部署独立保管
#
# 用法: cp .env.example .env && nano .env

# ──────────────────────────────────────────
# 必填: JWT 签名密钥
# ──────────────────────────────────────────
# 生成: openssl rand -hex 32  (或任意 64+ 字符强随机串)
# 警告: 不改 = 所有部署用同一弱 key, 任何人能伪造登录 token
JWT_SECRET=请改成强随机串_例如_用openssl_rand_hex_32_生成

# ──────────────────────────────────────────
# 可选: CORS 允许的 origin
# ──────────────────────────────────────────
# 默认 *  = 允许所有 (内网 / 单用户场景 OK)
# 公网部署填具体域名, 多个用逗号分隔
# 例: QUINK_ALLOWED_ORIGINS=https://quink.cc
QUINK_ALLOWED_ORIGINS=*

# ──────────────────────────────────────────
# 可选: 端口 (默认 38999)
# ──────────────────────────────────────────
# 改宿主机端口看 docker-compose.yml 的 ports 段, 不要改这个
# QUINK_PORT=38999

# ──────────────────────────────────────────
# 可选: 用 GHCR 预构建镜像 (推荐, 30 秒可用)
# ──────────────────────────────────────────
# 默认空 = 本地 build (15-25 min 首次, 之后走 docker cache 快)
# 设了 = pull GHCR 预构建镜像 30 秒可用, 不用 build
# QUINK_IMAGE=ghcr.io/mushroomscript/quink:latest

# ──────────────────────────────────────────
# 可选: 本地 build 加速 (仅 QUINK_IMAGE 空时生效)
# ──────────────────────────────────────────
# 默认空走官方源 (国外用户保持空). 中国大陆用户取消下面两行注释加速 build:
# APT_MIRROR=mirrors.tuna.tsinghua.edu.cn
# NPM_MIRROR=https://registry.npmmirror.com
